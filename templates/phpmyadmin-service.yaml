apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: phpmyadmin
  ports:
    - port: 80
      targetPort: {{ .Values.phpmyadmin.port }}